# Laptop-Price-Predictor
A Machine Learning–powered web application built with Flask, designed to estimate laptop prices based on hardware specifications such as RAM, weight, CPU, GPU, brand, and other features. The project uses a trained ML model and provides a simple, clean web interface for predictions.
